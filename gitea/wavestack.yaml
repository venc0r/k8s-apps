---
backup:
  volumes:
    - name: gitea-data
      persistentVolumeClaim:
        claimName: data-gitea-0
    - name: gitea-backup
      persistentVolumeClaim:
        claimName: data-gitea-0-backup

gitea:
  gitea:
    config:
      server:
        DOMAIN: gitea.ws.v3nc.org
