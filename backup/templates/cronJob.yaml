---
apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: {{ include "backup.fullname" . }}
  labels:
    {{- include "backup.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.schedule | quote }}
  jobTemplate:
    spec:
      template:
        metadata:
          name: {{ include "backup.fullname" . }}
        spec:
          affinity:
            podAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
              - labelSelector:
                  matchExpressions:
                    {{- toYaml .Values.matchExpressions | nindent 18 }}
                topologyKey: kubernetes.io/hostname
          initContainers:
          - name: fix-permissions
            securityContext:
              runAsUser: 0
            image: busybox
            command: ["sh", "-c", "chown {{ .Values.permissions.owner }}:{{ .Values.permissions.group }} {{ .Values.permissions.dir }}"]
            volumeMounts:
              {{- toYaml .Values.volumeMounts | nindent 12 }}
          containers:
          - name: {{ .Chart.Name }}
            securityContext:
              {{- toYaml .Values.securityContext | nindent 14 }}
            image: {{ .Values.image }}
            command:
            - sh
            - -c
            - {{ .Values.shellCommand | quote }}
            volumeMounts:
              {{- toYaml .Values.volumeMounts | nindent 12 }}
          volumes:
          {{- toYaml .Values.volumes | nindent 10 }}
          restartPolicy: OnFailure
          dnsPolicy: ClusterFirst
