apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nextcloud
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'ssh://git@gitea.ws.v3nc.org:2222/venc0r/k8s-apps.git'
    path: nextcloud
    targetRevision: HEAD
    plugin:
      name: argocd-vault-plugin-helm
      env:
        - name: PLUGIN_PARAMS
          value: "-f values.yaml -f values.openstack.yaml"
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: nextcloud
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
